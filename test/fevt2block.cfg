process BLOCK = {
    
    untracked PSet maxEvents = {untracked int32 input = 10}
    
    include "Configuration/StandardSequences/data/Reconstruction.cff"
    include "Configuration/StandardSequences/data/FakeConditions.cff" 
	include "FWCore/MessageService/data/MessageLogger.cfi"
    
 
    replace particleFlowCluster.verbose     = false
    replace particleFlowBlock.verbose       = false
    replace particleFlowSimParticle.verbose = false
    

    #service = Timing { }

    source = PoolSource {
        untracked vstring fileNames = { 'file:singletau_fevt.root' }
    }
    
/*
    service = MessageLogger  {
	untracked vstring destinations   = { "cout" }
	untracked vstring categories     = {
	    "PFClusterProducer",
	    "PFSimParticleProducer",
	    "PFBlockProducer" 
	}
	untracked vstring debugModules   = { "*" }
	untracked PSet cout = { 
	    untracked string threshold = "INFO"
	    untracked PSet INFO            = { untracked int32 limit = 0 }
	    untracked PSet DEBUG           = { untracked int32 limit = 0 }
	    untracked PSet PFClusterProducer = { 
		untracked int32 limit = 10000000
	    }
	    untracked PSet PFBlockProducer = { 
		untracked int32 limit = 10000000
	    }
	    untracked PSet PFSimParticleProducer = { 
		untracked int32 limit = 10000000
	    }
	} 
    }
*/
    ###############################
    #output module:
    module dump = EventContentAnalyzer {}
    module GEN-SIM-DIGI-RECO = PoolOutputModule
    {
	untracked string fileName = "singletau_full.root" 
	untracked vstring outputCommands = 
	{
            "drop *",
            "keep recoPFRecHits_*_*_*",
            "keep recoPFClusters_*_*_*",
            "keep recoPFRecTracks_*_*_*",
	    "keep recoPFBlocks_particleFlowBlock_*_*",
            "keep recoPFSimParticles_*_*_*",
            "keep recoTracks_*_*_*",
	    "keep recoCaloJets_*_*_*",
            "keep CaloTowersSorted_*_*_*",
	    "keep edmHepMCProduct_*_*_*"
	}
    }

    ###############################
    # module dump = EventContentAnalyzer {}

    path p = { 
#	dump,
	iterativeTk,
	caloTowersPFRec,
	particleFlowCluster,
	particleFlowTrack,
	particleFlowSimParticle,
	particleFlowBlock
    }

    endpath outpath = {GEN-SIM-DIGI-RECO}	

}
